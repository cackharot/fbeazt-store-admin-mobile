import {
    IOS_CLIENT_ID,
    WEB_CLIENT_ID,
    GCM_SENDER_ID,
    ANDROID_BASE_URL,
    IOS_BASE_URL,

    REFRESH_INTERVAL
} from 'react-native-dotenv';

export default {
    IOS_CLIENT_ID,
    WEB_CLIENT_ID,
    GCM_SENDER_ID,
    ANDROID_BASE_URL,
    IOS_BASE_URL,
    REFRESH_INTERVAL
};
